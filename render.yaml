services:
  - type: web
    name: courttime
    runtime: node
    rootDir: CourtTime Player View
    buildCommand: npm ci --include=dev && npm run build
    preDeployCommand: node scripts/run-migration.js --pending
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        sync: false  # Set this manually in Render dashboard
